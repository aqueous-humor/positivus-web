---
import Label from "../primitives/Label.astro";

const { title, subtitle, headingEl, variant } = Astro.props;

---

<div class="flex flex-row gap-10 items-center">
  <Label text={title} variant={variant} as={headingEl} />
  <p class="max-w-[580px] text-p leading-p">
    {subtitle}
  </p>
</div>